<p id="notice"><%= notice %></p>

<h1>Addresses</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Customer</th>
      <th>Street number</th>
      <th>Street name</th>
      <th>Pincode</th>
      <th>City</th>
      <th>Country</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @addresses.each do |address| %>
      <tr>
        <td><%= address.id %></td>
        <td><%= address.customer.name %></td>
        <td><%= address.street_number %></td>
        <td><%= address.street_name %></td>
        <td><%= address.pincode %></td>
        <td><%= address.city %></td>
        <td><%= address.country %></td>
        <td><%= link_to 'Show', address %></td>
        <td><%= link_to 'Edit', edit_address_path(address) %></td>
        <td><%= link_to 'Destroy', address, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Address', new_address_path %>

<h1>Soft Deleted Addresses</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Customer</th>
      <th>Street number</th>
      <th>Street name</th>
      <th>Pincode</th>
      <th>City</th>
      <th>Country</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @addresses_soft_deleted.each do |address| %>
      <tr>
        <td><%= address.id %></td>
        <td><%= address.customer.name %></td>
        <td><%= address.street_number %></td>
        <td><%= address.street_name %></td>
        <td><%= address.pincode %></td>
        <td><%= address.city %></td>
        <td><%= address.country %></td>
        <td><%= link_to 'Show', address %></td>
        <td><%= link_to 'Edit', edit_address_path(address) %></td>
        <td><%= link_to 'Destroy', address, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br><br>
<h2><a href="/">Customers List</a></h2>
